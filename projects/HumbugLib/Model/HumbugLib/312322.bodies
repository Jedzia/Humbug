class Txt
!!!298498.cpp!!!	upperCase(in s : string)
std::string upper(s);
for(size_t i = 0; i < s.length(); ++i)
   upper[i] = toupper(upper[i]);
return upper;
!!!298626.cpp!!!	lowerCase(in s : string)
std::string lower(s);
for(size_t i = 0; i < s.length(); ++i)
    lower[i] = tolower(lower[i]);
return lower;
